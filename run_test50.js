console.log("Wait, the user said:");
console.log("Bug affected layers are in a ghost like state (more blurry) or has a ghost like state somewhere else in the timeline");
console.log("Is it related to motion blur?");
console.log("If `PZ.motionBlur` uses a `WeakMap` mapped to `e` (which is a `THREE.Object3D`), and we duplicate the layer, does the duplicate share the same `THREE.Object3D` instances?");
